refactor: remove PIN lock, remove forgot-password, widen Settings and SyncLog

PIN lock
- Delete PinLock.tsx entirely (was idle-screen lock feature)
- Remove /pin route and AuthLayout wrapper from App.tsx
- Remove PinLock lazy import from App.tsx
- Strip pinLocked, lockPin, unlockPin from authStore.ts

Login
- Remove forgot-password flow (forgotMode state, form, sent state)
- Remove Forgot password button
- Remove CheckCircle2 import (unused after removal)
- Login page is now a clean single-purpose sign-in form

Settings page
- Remove max-w-2xl constraint — now fills the full content width
- Row 1: Store Information spans full width (3-col grid for phone/email/website)
- Row 2: Two-column layout (lg:grid-cols-2)
  Left:  Receipt Customization + Printer Settings
  Right: Date & Time + Authorization + (Inventory | Sync) side-by-side
- Section headers upgraded: icon now in a brand-pale pill badge
- Descriptions added to Receipt Header and Footer fields

SyncLog page
- Remove max-w-2xl constraint — now fills the full content width
- Replaced custom h1/p header with standard PageHeader component
- Stats cards upgraded: px-5 py-4, consistent typography
- Log entries: min-w-0 + truncate for long detail text on narrow screens

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
