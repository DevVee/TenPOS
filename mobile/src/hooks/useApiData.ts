// Re-export useApiData from the shared package.
// All existing imports of this file continue to work unchanged.
export { useApiData } from '@tenpos/shared'
export type { UseApiDataResult } from '@tenpos/shared'
