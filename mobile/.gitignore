# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings (contains sensitive permission rules)
.claude/settings.local.json

# Capacitor — generated native project (rebuild with: npm run cap:sync)
# Commit android/ only if you need CI/CD to build APKs without npm install
# android/

# ── Signing — NEVER commit these ─────────────────────────────────────────────
android/tenpos-release.jks
android/keystore.properties
*.jks
*.keystore
KEYSTORE_CREDENTIALS.txt
